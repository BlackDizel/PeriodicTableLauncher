<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    tools:background="#faa">

    <ScrollView
        android:id="@+id/svSettings"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginStart="16dp"
        android:visibility="gone"
        tools:visibility="visible">

        <LinearLayout
            android:layout_width="48dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center_vertical"
            android:orientation="vertical">


            <ImageView
                android:id="@+id/ivSettings"
                style="@style/settings_button"
                android:src="@drawable/ic_settings_white_24dp" />

            <ImageView
                android:id="@+id/ivAppShadow"
                style="@style/settings_button" />

            <ImageView
                android:id="@+id/ivAppColor"
                style="@style/settings_button" />

            <ImageView
                android:id="@+id/ivGridSize"
                style="@style/settings_button" />

            <ImageView
                android:id="@+id/ivSortMethod"
                style="@style/settings_button" />

            <ImageView
                android:id="@+id/ivSortOrientation"
                style="@style/settings_button"
                android:src="@drawable/ic_sort_ascend_white_24dp" />

            <ImageView
                android:id="@+id/ivBackground"
                style="@style/settings_button"
                android:src="@drawable/ic_image_white_24dp" />

        </LinearLayout>

    </ScrollView>

    <FrameLayout
        android:id="@+id/flSettingsButton"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:background="?attr/selectableItemBackground">

        <ImageView
            android:id="@+id/ivSettingsButton"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_gravity="center"
            android:padding="6dp"
            android:src="@drawable/ic_arrow_right_24dp" />

    </FrameLayout>

</LinearLayout>